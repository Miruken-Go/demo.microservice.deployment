name: Deploy Environment 

on: 
  workflow_dispatch:
    inputs:
      env:
        type: choice
        description: Env
        options: 
        - dev 
        - qa
        - staging
        - prod
      instance:
        description: 'Unique instance of an environment'
        default: ''
        required: false
        type: string

jobs:
  containerized_job:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/miruken-go/demo.microservice.deployment:1686001494
      env:
        tenantId:                       ${{ secrets.TENANT_ID }}
        subscriptionId:                 ${{ secrets.SUBSCRIPTION_ID }}
        deploymentPipelineClientId:     ${{ secrets.DEPLOYMENT_PIPELINE_CLIENT_ID }}
        deploymentPipelineClientSecret: ${{ secrets.DEPLOYMENT_PIPELINE_CLIENT_SECRET }}
        env:                            ${{ inputs.env }}
        instance:                       ${{ inputs.instance }}
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
    - name: pwd
      run:  pwd
    - name: ls
      run:  ls -la
    - name: deployEnvironment
      run:  node ./src/deployEnvironment.js 
